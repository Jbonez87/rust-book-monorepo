use std::collections::HashMap;
//nested path example
// use std::io::{self, Write};

//glob path example
// use std::collections::*;

fn main() {
  let mut map = HashMap::new();

  map.insert(1, 2);
  println!("{:?}", map);
}